<%= form_for(purchased_product) do |f| %>
  <% if purchased_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchased_product.errors.count, "error") %> prohibited this purchased_product from being saved:</h2>

      <ul>
      <% purchased_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.number_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :count %>
    <%= f.number_field :count %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
