<%= form_for(saved_product) do |f| %>
  <% if saved_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_product.errors.count, "error") %> prohibited this saved_product from being saved:</h2>

      <ul>
      <% saved_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.number_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :count %>
    <%= f.number_field :count %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
